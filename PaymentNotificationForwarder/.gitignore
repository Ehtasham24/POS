# Android Studio / Gradle build output and local machine state — none of this belongs in
# version control (build/ is regenerated by Gradle, local.properties has this machine's own
# SDK path, .idea/ is per-developer IDE state).
*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
